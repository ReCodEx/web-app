import React from 'react';
import FontAwesomeIcon from '@fortawesome/react-fontawesome';

const UploadIcon = props =>
  <FontAwesomeIcon {...props} icon="cloud-upload-alt" />;

export default UploadIcon;
