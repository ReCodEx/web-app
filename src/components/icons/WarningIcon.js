import React from 'react';
import FontAwesomeIcon from '@fortawesome/react-fontawesome';

const WarningIcon = props =>
  <FontAwesomeIcon {...props} icon="exclamation-triangle" />;

export default WarningIcon;
